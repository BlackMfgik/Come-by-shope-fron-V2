# Come-by-shope-fron-V2

Second iteration of the food ordering app, rebuilt as a React SPA with TypeScript. Added REST API integration, JWT auth, cart context, user account, light/dark theme, and order modal.

How to run:

git clone https://github.com/your-repo/come-by.git

cd come-by

npm install

```

Create a `.env` file:
```

VITE_API_URL=https://your-backend-url

npm run dev
